Image assets for VANTA FITNESS.

The current build uses curated remote placeholder image URLs from `src/data/gymData.js` so the project stays lightweight while preserving a premium cinematic gym aesthetic. Replace those URLs with local production assets here when brand photography is ready.
